#!/usr/bin/env bash
# Idle-lock toggle. State + notification owned by the Quickshell IdleLock
# service (shell/services/IdleLock.qml). This script is a thin wrapper
# kept for any non-keybind callers; the keybind goes direct via IPC.
exec qs ipc call idle toggle
