# Session environment — non-visual, not theme-controlled
# For cursor/toolkit/theme env vars, see envs.conf (rendered from template)

# ─── PATH ───
# Prepend arche scripts so keybindings, waybar, and autostart can use bare names
env = PATH,$HOME/.local/bin/arche:$PATH
