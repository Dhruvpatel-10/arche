# ─── Prefix ───
set -g prefix C-Space
set -g prefix2 C-b
bind C-Space send-prefix

# ─── General ───
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-kitty:Tc,*:RGB"
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough on
setw -g aggressive-resize on
set -g detach-on-destroy off

# ─── Splits ───
bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
bind x kill-pane

# ─── Pane navigation (Ctrl+Alt+Arrow, no prefix needed) ───
bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

# ─── Pane resize (prefix + Shift+Arrow, repeatable) ───
bind -r S-Left resize-pane -L 5
bind -r S-Down resize-pane -D 5
bind -r S-Up resize-pane -U 5
bind -r S-Right resize-pane -R 5

# ─── Zoom ───
bind z resize-pane -Z

# ─── Swap panes (prefix + { / }) ───
bind { swap-pane -U
bind } swap-pane -D

# ─── Even layout toggles (prefix + = for equal split) ───
bind = select-layout even-horizontal
bind + select-layout even-vertical

# ─── Windows (Alt+number, no prefix needed) ───
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

bind -n M-Left select-window -t -1
bind -n M-Right select-window -t +1
bind -n M-S-Left swap-window -t -1 \; select-window -t -1
bind -n M-S-Right swap-window -t +1 \; select-window -t +1

bind k kill-window
bind r command-prompt -I "#W" "rename-window -- '%%'"

# ─── Sessions ───
bind C new-session -c "#{pane_current_path}"
bind R command-prompt -I "#S" "rename-session -- '%%'"
bind K kill-session
bind S choose-tree -sZ
bind P switch-client -p
bind N switch-client -n
bind -n M-Up switch-client -p
bind -n M-Down switch-client -n

# ─── Copy mode (vi) ───
setw -g mode-keys vi
bind Enter copy-mode
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "wl-copy"
bind -T copy-mode-vi Escape send -X cancel

# ─── Status bar ───
set -g status-position top
set -g status-interval 5
set -g status-left-length 30
set -g status-right-length 50
set -g window-status-separator ""
set -gw automatic-rename on
set -gw automatic-rename-format '#{b:pane_current_path}'

# ─── Quick actions ───
bind f run-shell "tmux neww -n lazygit lazygit"
bind d run-shell "tmux neww -n lazydocker lazydocker"
bind b run-shell "tmux neww -n btop btop"

# ─── Reload ───
bind q source-file "$HOME/.config/tmux/tmux.conf" \; display "Config reloaded"

# ─── Colors (rendered from theme) ───
source-file -q "$HOME/.config/tmux/colors.conf"
