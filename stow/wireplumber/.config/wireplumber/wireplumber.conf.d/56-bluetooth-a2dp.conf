# Disable auto-switching to HFP/HSP when a communication app opens.
# Keeps earbuds in A2DP (high-quality) mode unless manually switched.
wireplumber.settings = {
  bluetooth.autoswitch-to-headset-profile = false
}
