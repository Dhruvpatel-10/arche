# /etc/systemd/logind.conf.d/99-arche.conf — managed by arche
# Explicit lid close behavior. Hypridle locks screen before sleep,
# this ensures the lid always triggers suspend (even if hypridle is not running).

[Login]
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend
HandleLidSwitchDocked=ignore
