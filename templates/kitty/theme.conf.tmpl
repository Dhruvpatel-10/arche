# Kitty theme — rendered by theme.sh, do not edit manually.
# Source: templates/kitty/theme.conf.tmpl

# ─── Base Colors ───

background ${COLOR_BG}
foreground ${COLOR_FG}
cursor     ${COLOR_CURSOR}
cursor_text_color ${COLOR_BG}
url_color  ${COLOR_ACCENT_ALT}
url_style  single

selection_background ${COLOR_ACCENT}
selection_foreground ${COLOR_BG}

# ─── Tab Bar ───

active_tab_background   ${COLOR_ACCENT}
active_tab_foreground   ${COLOR_BG}
inactive_tab_background ${COLOR_BG_SURFACE}
inactive_tab_foreground ${COLOR_FG_MUTED}
tab_bar_background      ${COLOR_BG_ALT}
tab_bar_margin_color    ${COLOR_BG_ALT}

# ─── Border ───

active_border_color   ${COLOR_ACCENT}
inactive_border_color ${COLOR_BORDER}
bell_border_color     ${COLOR_WARN}

# ─── Mark Colors ───

mark1_background ${COLOR_ACCENT}
mark1_foreground ${COLOR_BG}
mark2_background ${COLOR_ACCENT_ALT}
mark2_foreground ${COLOR_BG}
mark3_background ${COLOR_TEAL}
mark3_foreground ${COLOR_BG}

# ─── 16-Color Palette ───

# black
color0  ${COLOR_BG_SURFACE}
color8  ${COLOR_SURFACE2}

# red
color1  ${COLOR_CRITICAL}
color9  ${COLOR_CRITICAL}

# green
color2  ${COLOR_SUCCESS}
color10 ${COLOR_SUCCESS}

# yellow
color3  ${COLOR_WARN}
color11 ${COLOR_PEACH}

# blue
color4  ${COLOR_ACCENT_ALT}
color12 ${COLOR_SKY}

# magenta
color5  ${COLOR_PINK}
color13 ${COLOR_MAUVE}

# cyan
color6  ${COLOR_TEAL}
color14 ${COLOR_TEAL}

# white
color7  ${COLOR_SUBTEXT1}
color15 ${COLOR_FG}
