# Generated — do not edit. Edit templates/zathura/zathurarc-colors.tmpl and run: just theme

# ─── Default UI ───

set default-bg "${COLOR_BG}"
set default-fg "${COLOR_FG}"

# ─── Statusbar ───

set statusbar-bg "${COLOR_BG_ALT}"
set statusbar-fg "${COLOR_FG_MUTED}"

# ─── Input bar ───

set inputbar-bg "${COLOR_BG_SURFACE}"
set inputbar-fg "${COLOR_FG}"

# ─── Notifications ───

set notification-bg "${COLOR_BG_SURFACE}"
set notification-fg "${COLOR_FG}"
set notification-error-bg "${COLOR_BG_SURFACE}"
set notification-error-fg "${COLOR_CRITICAL}"
set notification-warning-bg "${COLOR_BG_SURFACE}"
set notification-warning-fg "${COLOR_WARN}"

# ─── Completion ───

set completion-bg "${COLOR_BG_SURFACE}"
set completion-fg "${COLOR_FG}"
set completion-group-bg "${COLOR_BG_ALT}"
set completion-group-fg "${COLOR_ACCENT}"
set completion-highlight-bg "${COLOR_ACCENT}"
set completion-highlight-fg "${COLOR_BG}"

# ─── Index (table of contents) ───

set index-bg "${COLOR_BG}"
set index-fg "${COLOR_FG}"
set index-active-bg "${COLOR_ACCENT}"
set index-active-fg "${COLOR_BG}"

# ─── Search highlight ───

set highlight-color "${COLOR_WARN}"
set highlight-active-color "${COLOR_ACCENT}"

# ─── Loading ───

set render-loading-bg "${COLOR_BG}"
set render-loading-fg "${COLOR_FG_MUTED}"

# ─── Recolor (dark-mode PDF reading) ───

set recolor-lightcolor "${COLOR_BG}"
set recolor-darkcolor "${COLOR_FG}"

# ─── Font ───

set font "${FONT_SANS} ${FONT_SIZE_NORMAL}"
